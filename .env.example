# Copy this file to `.env` and fill in your keys (never commit `.env`).
# OpenAI — used by CrewAI / agents (gpt-4o).
OPENAI_API_KEY=
# SerpApi — Google Flights & Google Hotels tools.
SERPAPI_API_KEY=
# Serper — SerperDevTool web search.
SERPER_API_KEY=
